SELECT COUNT(CO.PIZZA_ID) AS NUMBER_OF_PIZZAS
,CO.ORDER_ID
FROM RUNNER_ORDERS AS RO
INNER JOIN CUSTOMER_ORDERS AS CO
ON CO.ORDER_ID = RO.ORDER_ID
WHERE PICKUP_TIME != 'null'
GROUP BY CO.ORDER_ID
ORDER BY COUNT(CO.PIZZA_ID) DESC
LIMIT 1